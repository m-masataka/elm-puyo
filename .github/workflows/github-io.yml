name: Deploy Github.io

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    container: codesimple/elm:0.19

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: elm make --output=main.js src/Main.elm

      - name: saving data
        run: cp -r ./ /tmp/code
      
      - uses: actions/checkout@v2
        with:
          ref: gh-pages

      - name: git setting
        run: |
          git config --local user.email "m.mizukoshi.wakuwaku@gmail.com"
          git config --local user.name "m-masataka"

      - name: copy files
        run: |
          cp /tmp/main.js ./main.js
          cp /tmp/src/main.css ./src/main.css
          cp -r /tmp/api ./api
          cp /tmp/index.html ./index.html

      - name: Commit files
        run: |
          git add .
          git commit -m "update" -a
          git push origin gh-pages